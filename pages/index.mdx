Welcome to Mathlore.

This is $x \in X$ some text.

This is

$$a \in A$$

more text.

```yaml
[\abc]
Describes: f(x)
where: 'something'
suchThat: "abc"
```

<div style={{ display: 'flex', flexDirection: 'column', }}>
<div><span style={{ fontFamily: 'courier', }}>[\continuous.function:on\{A * B\}:to\{B\}]</span></div>
<div><span style={{ fontFamily: 'courier', fontWeight: 'bold', }}>Result:</span> $f(x)$</div>
<div>&bull;&nbsp;<span style={{ fontFamily: 'courier', fontWeight: 'bold', }}>where:</span> $x \in X$</div>
<div>&nbsp;&nbsp;&bull;&nbsp;<span style={{ fontFamily: 'courier', fontWeight: 'bold', }}>where:</span> $x \in X$</div>
</div>
